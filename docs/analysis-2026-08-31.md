# SacredSpace — Project Analysis (August 31, 2026 — Updated September 12, 2026)

> Archived improvement audit. The live tracking docs are
> [`ARCHITECTURE.md`](./ARCHITECTURE.md) (known gaps) and
> [`SESSION-NOTES.md`](./SESSION-NOTES.md) (session handoffs).
> Originally `last_31_aug.md` at the repo root; filed here on 2026-09-12.

---

## Stack

TanStack Start + React 19 + Tailwind v4 + Clerk + Tiptap + Drizzle ORM + SQLite + PWA

---

## What's Good

- Clean project structure with file-based routing
- Strong accessibility (skip-to-content, ARIA labels, semantic HTML, 44px touch targets)
- PWA with offline support via service worker
- Well-designed admin panel with Clerk auth
- "Mantra of the Day" feature
- Flexible view modes (Sanskrit, transliteration, translation)
- Full audio player with speed/volume/loop controls
- Dark mode with localStorage persistence
- Server-side OG image generation (Satori)
- Test coverage (11 test files, 86 tests)
- Auto-migration + auto-seed for database (runs once at startup)
- Dependabot for automated dependency updates (added Sept 12)

---

## 🔴 Critical Issues

### 1. ~~XSS Vulnerability~~ ✅ Fixed

Server-side HTML sanitization via `sanitize-html` in `admin.ts`. DOMPurify available client-side in `ProseRenderer.tsx`.

### 2. ~~Typecheck Fails~~ ✅ Fixed

`npx tsc --noEmit` passes cleanly with zero errors.

### 3. ~~`validateEnv()` Never Called~~ ✅ Fixed (Sept 5)

`validateEnv()` is now called at module load in `src/server/db/index.ts`. Runs once per cold start before DB connection or seeding. In development, throws immediately if `VITE_CLERK_PUBLISHABLE_KEY` or `CLERK_SECRET_KEY` are missing. In production, logs the error.

---

## 🟡 High-Impact Improvements

### 4. ~~Duplicated Search Logic~~ ✅ Fixed

`searchContents` in `server/functions/contents.ts` is the canonical implementation. The search route imports it directly.

### 5. ~~Duplicated Admin CRUD~~ ✅ Fixed (verified Sept 12)

`admin.dashboard.tsx` imports `toggleContentStatus` and `deleteContent` from `server/functions/admin.ts` — no inline duplicates remain.

### 15. Duplicated `getAllDeities` ✅ Fixed

Removed unused duplicate from `deities.ts`. Canonical version in `contents.ts` used by all routes and tests.

### 6. ~~No Input Validation on Search~~ ✅ Fixed

All 19 server function inputs now validated with Zod schemas (`validators.ts`). Slugs enforce kebab-case, strings have length limits, IDs must be positive integers, arrays capped at 100 items.

### 7. ~~DB Seeding Runs N+1 Queries~~ ✅ Fixed

Already uses batch insert (`db.insert(deities).values(seedDeities)`) and maps slugs to IDs from returning rows.

### 4b. ~~Duplicated Server Functions in Mantra/Deity Routes~~ ✅ Fixed (Sept 5)

`mantra.$slug.tsx` and `deity.$slug.tsx` previously defined their own inline `createServerFn` handlers with dynamic imports and `ensureSeeded()` calls. Now all four functions are centralized in `server/functions/contents.ts`:

- `getContentBySlugWithDeity` — returns `{ content, deity }` by slug (with rate limiting + `NotFoundError`)
- `getSiblingContent` — returns published siblings for a deity excluding one slug
- `getDeityBySlug` — returns a deity by slug (with rate limiting + `NotFoundError`)
- `getContentsByDeitySorted` — returns all contents for a deity sorted by type then title

Both routes now import from the centralized module. ~55 lines of duplicated code removed.

---

## 🟢 Quality & Feature Improvements

### 8. ~~Structured Data (Schema.org)~~ ✅ Implemented

`WebSite` schema on homepage with `SearchAction`. `Article` schema on mantra pages with `headline`, `datePublished`, `author`, `publisher`, `about`, `inLanguage`, `keywords`. Generated in route `head()` functions.

### 9. ~~Expand Test Coverage~~ ✅ Improved (Sept 12)

11 test files with **86 tests**:
- `DeityCard.test.tsx` (5 tests)
- `Toast.test.tsx` (4 tests)
- `daily.test.ts` (2 tests)
- `likes.test.ts` (6 tests)
- `contents.test.ts` (21 tests)
- `admin.test.ts` (14 tests)
- `deities.test.ts` (13 tests)
- `saved.test.ts` (4 tests)
- `analytics.test.ts` (3 tests)
- `storage.test.ts` (6 tests) — NEW Sept 12 (Blob cleanup)
- `rate-limit.test.ts` (8 tests) — NEW Sept 12 (durable limiter)

Still missing: search route, auth flows, audio player, breadcrumbs, skeleton components.

### 10. ~~Error Boundaries~~ ✅ Implemented (verified Sept 12)

All 14 route files define `errorComponent`, plus a root-level `Application Error` fallback in `__root.tsx`. `admin.dashboard.tsx` differentiates 401 (sign-in prompt) from other errors.

### 18. Custom Error Classes ✅ Implemented

`src/lib/errors.ts` provides `AppError`, `UnauthorizedError` (401), `NotFoundError` (404), `RateLimitError` (429), and `ConflictError` (409). Routes/error boundaries can now catch `instanceof AppError` and switch on `statusCode`.

### 11. ~~Search Debounce~~ ✅ Implemented

Search input debounced at 300ms via `useDebouncedCallback` custom hook. Typing triggers server re-fetch only after 300ms of inactivity.

### 12. ~~Internationalization (`lang` attributes)~~ ✅ Implemented (Sept 12)

- Mantra reader: `lang={bodyLang}` on the content container — `"sa"` for the Sanskrit (Devanagari) view, `"en"` for transliteration/translation views.
- `ProseRenderer` gained an optional `lang` prop, passed through from the mantra reader.
- Tiptap admin editor: Devanagari content area wrapped in `lang="sa"`.

(UI chrome remains English-only; full i18n of UI strings is out of scope.)

### 13. ~~Rate Limiting~~ ✅ Implemented — now DURABLE (Sept 12)

- Admin endpoints: 30–60 req/min per user
- Like toggling: 30 req/min per session
- Public endpoints (deities, contents, search, daily, saved): 120 req/min per IP via `x-forwarded-for`

**Sept 12: durable backend added.** When `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` are set, counters live in Redis via Upstash's REST API (INCR → EXPIRE NX → PTTL, 1s timeout, **no SDK dependency**) and hold across serverless instances and cold starts. Otherwise a per-process in-memory Map is used (best-effort bursts only). The durable backend **fails open**: on Redis errors or timeouts it falls back to the in-memory counter for that request and logs a warning once per process. `checkRateLimit`/`enforceRateLimit` are now `async`; all 8 call sites await them.

### 14. ~~Image Optimization (lazy loading + layout stability)~~ ✅ Partially implemented (Sept 12)

All three `<img>` render sites (`DeityCard`, deity page, admin deities) already carry `width`/`height`, `loading="lazy"`, and `decoding="async"` — lazy loading and layout stability are done. Still open: responsive `srcset` and WebP/AVIF conversion at upload time.

### 14b. ~~Search uses LIKE~~ ✅ FTS5 implemented (Sept 12)

- `contents_fts` FTS5 virtual table (external-content, on title/description/transliteration/translation) with insert/delete/update triggers, created during DB bootstrap and rebuilt at startup.
- `searchContents` uses `MATCH` with BM25 `rank` ordering; type/deity filters and `sortBy=newest` applied in SQL. Query terms are double-quoted + prefix-starred (injection-safe). Automatic LIKE fallback if FTS is unavailable. API shape unchanged.

### 14c. Image Optimization (responsive srcset / WebP)

Still open — requires generating resized variants at upload time (e.g. via Vercel Blob image pipeline or `sharp` in a server function).

### 16. `ensureSeeded()` Called Per Request ✅ Fixed

Previously every server function called `ensureSeeded()` (DB migration + seed check). Now runs once at module load time — once per cold start, not per request.

### 17. `getMantraOfDay` Loads All Rows ✅ Fixed

Previously fetched every content row into memory to pick one. Now uses `COUNT` + `LIMIT/OFFSET` to fetch exactly 1 row, with deity joined in a single query.

### 19. ~~Audio Player Accessibility~~ ✅ Improved (Sept 5)

`AudioProvider.tsx` updated with comprehensive accessibility:
- `role="region"` + `aria-label="Audio player"` on the container
- Progress bar: `role="slider"` with `tabIndex={0}`, `aria-valuemin/max/now/text`, keyboard navigation (ArrowLeft/Right ±5%, Home/End), focus-visible ring
- Track info: `aria-live="polite"` announces new track to screen readers
- Time display: `aria-hidden` (redundant with slider's `aria-valuetext`)
- Speed buttons: `aria-pressed` + `aria-label="Speed 1.5x"`
- Volume slider: `aria-labelledby` + `aria-valuetext="75%"`
- Loop button: `aria-pressed` + `aria-label="Loop"`

### 20. ~~Font Preconnect~~ ✅ Implemented (Sept 5)

Added `<link rel="preconnect">` for `fonts.googleapis.com` and `fonts.gstatic.com` (with `crossOrigin="anonymous"`) in `__root.tsx`. Fonts already use `font-display: swap`. Typical savings: 50–150ms on first paint.

### 21. ~~CSP Headers Tightened~~ ✅ Implemented (Sept 5)

Content-Security-Policy in `vercel.json` updated with:
- `worker-src 'self'` — restricts service worker loading to same-origin
- `base-uri 'self'` — prevents `<base href>` injection attacks

---

## 🆕 September 12, 2026 Session

### 22. ~~Docs Drift: SETUP.md contradicted production behavior~~ ✅ Fixed (Sept 12)

- `sacredspace/SETUP.md` §6 rewritten: replaced the stale `/tmp/sacredspace.db` cold-start diagram with the real module-load lifecycle (`validateEnv` → `createClient` → memoized `ensureSeeded`) and the production fail-fast rule (throws without `TURSO_DATABASE_URL`).
- §7: deploy branch `master` → `main`; env var list now includes `TURSO_DATABASE_URL`/`TURSO_AUTH_TOKEN` (required) and `BLOB_READ_WRITE_TOKEN` (optional); `nodejs24.x` claim replaced with Node 22 from `.nvmrc`/`engines`.
- §8: two obsolete architecture-decision rows replaced (Turso fail-fast; `src/lib/storage.ts` uploads).
- §10 troubleshooting: fixed three wrong rows (`/tmp` ENOENT advice, `NEXT_PUBLIC_CLERK_*` → `VITE_CLERK_*`, "images committed to git" → Blob token); dropped the "DB resets on cold start" row.
- `sacredspace/.env.example`: Turso corrected from "optional, falls back to /tmp" to "required in production, fails fast"; Upstash vars documented.

### 23. ~~Blob Orphan Cleanup~~ ✅ Implemented (Sept 12)

Replacing a deity image or audio file used to leak the previous Vercel Blob object forever.

- `src/lib/storage.ts`: new `deleteStoredFile(url)` — best-effort delete of previously stored files. Blob URLs (`*.public.blob.vercel-storage.com`) deleted via `@vercel/blob`'s `del()` **only when a Blob token is configured** (same gate as `put()`, so a different Blob store is never touched). Deliberately Blob-only: seed images in `public/uploads/` are git-committed, and production filesystems are read-only. Failures logged, never thrown.
- Wired into `updateDeityImage` (replace), `removeDeityImage`, `deleteDeity` (also cleans contents' audio Blobs), `uploadContentAudio` (replace), `removeContentAudio`.
- 6 unit tests in `src/lib/__tests__/storage.test.ts`.

### 24. Dependabot ✅ Added (Sept 12)

`.github/dependabot.yml`: weekly grouped npm updates for `sacredspace/` (minor+patch grouped, majors as individual PRs) and weekly GitHub Actions updates.

### 25. Workspace Hygiene ✅ (Sept 12)

- `last_31_aug.md` relocated from repo root to `docs/analysis-2026-08-31.md` (this file).
- `docs/SESSION-NOTES.md` stale checklist items ticked off; `docs/README.md` documentation map updated.
- `docs/ARCHITECTURE.md` §8 known-gaps updated: Blob cleanup and rate limiting moved to the resolved list.

### Remaining Known Gaps (after Sept 12)

- **Responsive image variants** — `srcset`/WebP generation at upload time still open (lazy loading + dimensions done, item #14).
- **Durable rate limiting is opt-in** — set the Upstash env vars in production to activate it (item #13).
- **FTS5 index verified by unit tests only** — run a real search against the dev server before relying on it in production.
- **Turso + Blob credentials** still need to be configured in Vercel project settings (see `docs/SESSION-NOTES.md` action items).

---

## 📊 Current State Summary

| Area | Status |
|---|---|
| Planning docs (PRD, UX, Architecture) | ✅ Complete |
| Visitor routes (home, deity, mantra, search, saved) | ✅ Built |
| Admin routes (dashboard, editor, deities, analytics) | ✅ Built |
| Auth (Clerk sign-in/sign-up) | ✅ Fixed & deployed |
| Audio player | ✅ Built |
| Audio player accessibility | ✅ ARIA roles, keyboard nav, aria-live |
| PWA / Offline | ✅ Built |
| OG images | ✅ Built |
| Dark mode | ✅ Built |
| Design system | ✅ Polished |
| Database (auto-seed + migration) | ✅ Working |
| GitHub remote | ✅ github.com/jawaleketan/sacredspace |
| Vercel deployment | ✅ Live at sacredspace.vercel.app |
| Tests | ✅ 11 files, 86 tests |
| Security (XSS, input validation, CSP) | ✅ Sanitized + Zod validated + CSP headers |
| Rate limiting | ✅ Public + admin + likes — durable (Upstash) with in-memory fallback |
| Content search | ✅ FTS5 full-text (BM25-ranked) with LIKE fallback |
| Blob orphan cleanup | ✅ Replaced/removed uploads are deleted (Blob-only) |
| Error handling | ✅ Custom error classes (401/404/409/429) |
| SEO (structured data, meta) | ✅ WebSite + Article schemas |
| Code deduplication | ✅ Major duplicates removed (mantra/deity routes centralized) |
| TypeScript strictness | ✅ Typecheck passes clean |
| Input validation | ✅ Zod schemas on all 19 endpoints |
| Env validation | ✅ validateEnv() called at module load |
| Performance (fonts) | ✅ Preconnects + font-display swap |
| Internationalization (content) | ✅ `lang="sa"` on Devanagari sections |
| Docs accuracy | ✅ SETUP.md matches production fail-fast behavior |
| Dependency updates | ✅ Dependabot (weekly, grouped) |
| Lint | ✅ ESLint 9 in CI (typecheck → lint → tests) |

**Verification (Sept 12):** `tsc --noEmit` clean · `eslint .` 0 problems · 86/86 tests pass.
